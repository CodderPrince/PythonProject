#include <bits/stdc++.h>  // You can keep this for convenience

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);  // Disable synchronization with C streams
    cin.tie(nullptr);  // Untie cin from cout to improve I/O performance

    // Direct output without flushing
    cout << "GCC setup is successful!" << "\n";
    
    return 0;
}
