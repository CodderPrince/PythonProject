#include <stdio.h>

    int main()
    {
         int a, b, c, tmp;
         scanf("%d %d %d", &a, &b, &c);
         int *x,*y,*z;
         *x=a;
         *y=b;
         *z=c;
         if(*x>*y && *x>*z)
         {
            int temp=*x;
         }
         else if("")
        printf("%d eh o maior\n", tmp);
        return 0;
    }    