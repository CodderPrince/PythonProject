#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    printf("%d\n",sum(a,b));
    return 0;
}

int sum(a,b)
{
    if(a==0 && b==0)
    {
        return 0;
    }
    else
    {
        return sum(a+b);
    }
}
